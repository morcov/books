<%= form_for @path do |f| %>
  <% if @path.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@path.errors.count, "error") %> prohibited
      this path from being saved:</h2>
    <ul>
    <% @path.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
    <% if @book %>
     <%= f.hidden_field :book_id, :value => @book.id %>
    <% end %>
  <p>
    <%= f.label :path %><br>
    <%= f.text_field :path %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>

    <%= submit_tag "Save" %>
  </p>
<% end %>