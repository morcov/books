
<p>
  <strong> <%= @path.book %></strong>
  <strong>path:</strong>
  <%= @path.path %>
 </p>
 <p>
  <%= simple_format(@path.text) %>
</p>
<p>
    <%= link_to 'Back', book_path(@path.book_id) %>
  <% if current_user %>
   <% if current_user.id == @path.user_id ||  current_user.rank_id == 2 %>
    | <%= link_to 'Edit', edit_path_path(@path) %> |
    <%= link_to 'Destroy', path_path(@path),
                    method: :delete, data: { confirm: 'Are you sure?' } %>
   <% end %>
  <% end %>
</p>